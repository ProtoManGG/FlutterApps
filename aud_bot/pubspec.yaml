name: aud_bot
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  alan_voice: ^2.0.11
  audioplayers: ^0.17.0
  cupertino_icons: ^1.0.0
  flutter_dotenv: ^2.1.0
  get: ^3.22.2
  google_fonts: ^1.1.1
  velocity_x: ^1.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/
    - .env
